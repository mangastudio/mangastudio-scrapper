# mangastudio-uploader

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build mangastudio-uploader` to build the library.

## Running unit tests

Run `nx test mangastudio-uploader` to execute the unit tests via [Jest](https://jestjs.io).
